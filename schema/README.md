# Schemas

Machine-readable tool and result definitions.

**Licensed Apache-2.0** (`LICENSE` in this directory), separately from the specification text,
so you can copy these straight into your codebase.

> v0.1: empty on purpose. These are being extracted from the reference implementation as its
> tools are built and proven in the field, rather than written speculatively. A schema that has
> never run against a real device is a guess.
